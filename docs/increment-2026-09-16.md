# Increment 2026-09-16

This branch continues the mobile-flow phase from `foundation-clean`.

Scope: integrate the existing notification foundation with ticket transfer lifecycle events. No provider credentials are required; the existing mock provider remains the default and FCM remains selectable by configuration.
